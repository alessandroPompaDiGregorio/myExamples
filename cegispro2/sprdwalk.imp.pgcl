python3 -m cegispro2.cmd sprdwalk.imp.pgcl --invarianttype past --templaterefiner inductivity --distance 1 --initialstates "[x < n]"

nat x;
nat n;
nat r;
while (x < n) {
    r := 0 : 1/2 + 1 : 1/2;
    x := x + r;
    #tick(1);
}
